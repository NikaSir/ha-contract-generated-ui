# Generated Subpanel Shell v1 — manifest starter

This template is for **Contract Generated UI-owned Lovelace subpanels**.

Do not copy Header JavaScript or Bottom Tab Bar code into a domain integration. Add a manifest and reference a logical parent from `navigation/main.yaml`.

Use `panel-manifest.example.yaml` as a starter. Replace the placeholder renderer with real verified contract modules only when semantic inventory bindings exist.

See `docs/GENERATED_SUBPANEL_SHELL_V1.md`.
