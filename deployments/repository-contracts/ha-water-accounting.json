{
  "schema_version": 1,
  "repository": "NikaSir/ha-water-accounting",
  "source_revision": "8f73632eedc1eb2bb103fdc1cc22ae9ca39dc208",
  "kind": "ha_integration",
  "artifacts": [
    {
      "id": "panel",
      "path": "custom_components/water_accounting/frontend/water-accounting-panel.js",
      "ui_version": "0.1.2",
      "bindings": [
        {
          "role": "entrypoint",
          "path": "custom_components/water_accounting/panel.py",
          "pattern": "(?m)^\\s*(?:const\\s+)?PANEL_BUNDLE\\s*=\\s*[\\\"\\x27](?P<value>[^\\\"\\x27\\r\\n]+)[\\\"\\x27]",
          "expected": "water-accounting-panel.js",
          "path_mode": "basename"
        },
        {
          "role": "ui_version",
          "path": "custom_components/water_accounting/frontend/water-accounting-panel.js",
          "pattern": "(?m)^\\s*uiVersion\\s*:\\s*[\\\"\\x27](?P<value>[^\\\"\\x27\\r\\n]+)[\\\"\\x27]",
          "expected": "0.1.2"
        },
        {
          "role": "cache_key",
          "path": "custom_components/water_accounting/const.py",
          "pattern": "(?m)^\\s*(?:const\\s+)?UI_VERSION\\s*=\\s*[\\\"\\x27](?P<value>[^\\\"\\x27\\r\\n]+)[\\\"\\x27]",
          "expected": "0.1.2"
        }
      ],
      "registration_evidence_paths": [
        "custom_components/water_accounting/__init__.py",
        "custom_components/water_accounting/panel.py"
      ],
      "binding_limitations": []
    }
  ],
  "domain": "water_accounting",
  "manifest_path": "custom_components/water_accounting/manifest.json",
  "standards": {
    "applicable": true,
    "reason": "Active NikaS panel UI.",
    "evidence_paths": [
      "custom_components/water_accounting/__init__.py"
    ],
    "declaration_path": ".nikas-ui-standard.json",
    "observed_version": "1.9",
    "required_version": "2.2"
  },
  "publication": {
    "default_branch": "main",
    "github_releases": false,
    "automatic_tags": false
  },
  "observed_workflow_paths": [
    ".github/workflows/integration-validation.yml",
    ".github/workflows/repository-checks.yml"
  ],
  "evidence": [
    {
      "requirement": "data_quality",
      "status": "pending",
      "paths": [],
      "note": "A18: partial meter coverage must not be displayed as a complete sum."
    },
    {
      "requirement": "command_safety",
      "status": "pending",
      "paths": [],
      "note": "No complete acceptance evidence for the pinned production runtime is recorded. Existing tests are inventory only."
    },
    {
      "requirement": "lifecycle",
      "status": "pending",
      "paths": [],
      "note": "No complete acceptance evidence for the pinned production runtime is recorded. Existing tests are inventory only."
    },
    {
      "requirement": "repository_checks",
      "status": "pending",
      "paths": [
        ".github/workflows/integration-validation.yml",
        ".github/workflows/repository-checks.yml",
        "tests/test_contract.py",
        "scripts/check-panel.mjs"
      ],
      "note": "Existing workflow/test paths are observed inventory, not proof of production coverage or runtime acceptance. Remote required checks have not been captured in this profile."
    },
    {
      "requirement": "device_acceptance",
      "status": "pending",
      "paths": [],
      "note": "No complete acceptance on the running user Home Assistant, iPhone, or target gateway hardware is recorded for this pinned revision."
    }
  ],
  "findings": [
    {
      "id": "A18",
      "requirement": "data_quality",
      "status": "open",
      "note": "Partial meter sums appear complete."
    },
    {
      "id": "A19",
      "requirement": "standards",
      "status": "open",
      "note": "Observed declaration remains 1.9; required baseline is 2.2. No version-only migration or waiver."
    }
  ]
}
